# ============================================================================
# TELEGRAM UPTIME MONITORING BOT - FINAL DEPENDENCIES
# ============================================================================

# --- Core Bot Framework ---
# Telegram API handling using Aiogram
aiogram==3.3.0
aiohttp==3.9.1
aiofiles==23.2.1

# --- Database Layer (Async Support) ---
# Supports both SQLite (Local) and PostgreSQL (Production/Render)
sqlalchemy==2.0.23
aiosqlite==0.19.0
asyncpg==0.29.0
psycopg2-binary==2.9.9

# --- Monitoring & Networking ---
# Essential for pinging sites, DNS checks and URL validation
httpx==0.25.2
dnspython==2.4.2
validators==0.22.0

# --- Configuration & Validation ---
# Handles .env file and settings validation
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# --- Logging & Performance ---
# Professional logging and system metrics
loguru==0.7.2
psutil==5.9.6

# --- Render Specifics ---
# Server for health checks and keep-alive
uvicorn==0.24.0
